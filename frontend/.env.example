# Local dev: leave unset — Vite proxies /api to http://localhost:5000
# Production: your deployed Flask backend on Render (include /api, no trailing slash)
# VITE_API_URL=https://employee-salaryslip-api.onrender.com/api

# Local dev: leave unset — Vite proxies /api to http://localhost:5000
# Production: your deployed Flask backend on Vercel (include /api, no trailing slash)
# VITE_API_URL=https://your-backend.vercel.app/api
